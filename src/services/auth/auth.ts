const createUserService = async (user: any) => {
    console.log(user);
}

const updateUserService = async (user: any) => {
    console.log(user);
}

export {
    updateUserService,
    createUserService,
}
